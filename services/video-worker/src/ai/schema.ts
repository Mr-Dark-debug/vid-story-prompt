import { z } from "zod";
export const clipCandidateSchema = z.object({ startSeconds: z.number().nonnegative(), endSeconds: z.number().positive(), title: z.string().min(1).max(120), hook: z.string().min(1).max(240), summary: z.string().min(1).max(500), topic: z.string().min(1).max(120), transcriptExcerpt: z.string().min(1), standaloneScore: z.number().min(0).max(100), hookScore: z.number().min(0).max(100), clarityScore: z.number().min(0).max(100), storyScore: z.number().min(0).max(100), relevanceScore: z.number().min(0).max(100), overallScore: z.number().min(0).max(100), explanation: z.string().min(1).max(600) }).refine((item) => item.endSeconds > item.startSeconds, "End must be after start");
export const clipPlanningResponseSchema = z.object({ candidates: z.array(clipCandidateSchema).max(100) });
