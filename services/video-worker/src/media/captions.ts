export function formatSrtTime(seconds: number) { const ms=Math.max(0,Math.round(seconds*1000));const hours=Math.floor(ms/3_600_000);const minutes=Math.floor(ms%3_600_000/60_000);const secs=Math.floor(ms%60_000/1000);const millis=ms%1000;return `${String(hours).padStart(2,"0")}:${String(minutes).padStart(2,"0")}:${String(secs).padStart(2,"0")},${String(millis).padStart(3,"0")}`; }
export function formatVttTime(seconds: number) { return formatSrtTime(seconds).replace(",","."); }
export function createSrt(text: string, duration: number) { const words=text.trim().split(/\s+/);const cues=[];const perCue=8;for(let index=0;index<words.length;index+=perCue){const n=index/perCue;const start=n*Math.max(1.2,duration/Math.ceil(words.length/perCue));const end=Math.min(duration,(n+1)*Math.max(1.2,duration/Math.ceil(words.length/perCue)));cues.push(`${n+1}\n${formatSrtTime(start)} --> ${formatSrtTime(end)}\n${words.slice(index,index+perCue).join(" ")}\n`);}return cues.join("\n"); }
export function createVtt(text: string, duration: number) { return `WEBVTT\n\n${createSrt(text,duration).replace(/^\d+\n/gm,"").replace(/,/g,".")}`; }
export function createAss(text: string, duration: number) { const escaped=text.replace(/[{}]/g,"").replace(/\n/g,"\\N");return `[Script Info]\nScriptType: v4.00+\nPlayResX: 1080\nPlayResY: 1920\nWrapStyle: 0\n\n[V4+ Styles]\nFormat: Name, Fontname, Fontsize, PrimaryColour, SecondaryColour, OutlineColour, BackColour, Bold, Italic, Underline, StrikeOut, ScaleX, ScaleY, Spacing, Angle, BorderStyle, Outline, Shadow, Alignment, MarginL, MarginR, MarginV, Encoding\nStyle: Default,Liberation Sans,64,&H00FFFFFF,&H0000A8FF,&H00101010,&H80000000,-1,0,0,0,100,100,0,0,1,4,1,2,80,80,180,1\n\n[Events]\nFormat: Layer, Start, End, Style, Name, MarginL, MarginR, MarginV, Effect, Text\nDialogue: 0,0:00:00.00,${assTime(duration)},Default,,0,0,0,,${escaped}\n`; }
function assTime(seconds:number){const cs=Math.round(seconds*100);const hours=Math.floor(cs/360000);const minutes=Math.floor(cs%360000/6000);const secs=Math.floor(cs%6000/100);return `${hours}:${String(minutes).padStart(2,"0")}:${String(secs).padStart(2,"0")}.${String(cs%100).padStart(2,"0")}`;}
