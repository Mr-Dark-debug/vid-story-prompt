import { describe,expect,it } from "vitest";
import { commitUsage,idempotencyKey,releaseUsage,reserveUsage } from "./usage";
describe("usage accounting",()=>{it("reserves commits and releases without going negative",()=>{const reserved=reserveUsage({limit:3600,reserved:0,committed:0},600);expect(reserved.reserved).toBe(600);expect(commitUsage(reserved,600)).toEqual({limit:3600,reserved:0,committed:600});expect(releaseUsage(reserved,600).reserved).toBe(0);});it("rejects overspend and invalid release",()=>{expect(()=>reserveUsage({limit:10,reserved:0,committed:0},11)).toThrow("insufficient_usage");expect(()=>releaseUsage({limit:10,reserved:1,committed:0},2)).toThrow();});it("creates deterministic idempotency keys",()=>expect(idempotencyKey("job","abc",1)).toBe("job:abc:1"));});
